/// <reference path="../test-typings.d.ts"/>
function withWarnFunc(warnFunc, execFunc) {
    var oldConsoleWarn = console.warn;
    try {
        console.warn = warnFunc;
        execFunc();
    }
    finally {
        console.warn = oldConsoleWarn;
    }
}
exports.withWarnFunc = withWarnFunc;
